[package]
name = "client"
version = "0.1.0"
edition = "2024"

[dependencies]
[target.'cfg(any(target_os = "windows"))'.dependencies]
winfsp = "0.11.3"
[target.'cfg(any(target_os = "linux"))'.dependencies]
fuser = "0.15.1"
